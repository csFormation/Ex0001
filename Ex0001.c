/*
 * Esto es una prueba
 */

#include "funciones.h"
#include <stdio.h>

int main ( void ) {
	int num = 4;
	
	printf( "El cuadrado de 4 es %d\n", cuadrado( num ) );
	printf( "El cuadrado de 4 es %d\n", potencia( num ) );
	
	return 0;
}
